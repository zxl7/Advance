var passengers = [{ name: 'bob', paid: ture }, { name: 'bob', paid: ture }, { name: 'bob', paid: ture }, { name: 'bob', paid: ture }];


function createDrink(passenger) {
    var orderFunction;
    if (passengers.ticket === "firstlclass") {
        orderFunction = function() {
            alert('apple and wine?');
        } else {
            orderFunction = function() {
                alert('apple and wine?');
            }
        }
    }
}

function create (passengers) {
	var orderFunction;
	if (passengers.ticket === "firstlclass") {
	 	orderFunction = function () {
	 		alert("toudengcang ");
	 	}else if(passengers.ticket === "premium"){
	 		orderFunction = function () {
	 		alert("hanghua ");;
	 	}else {
	 		orderFunction = function () {
	 		alert("jinjicang ");
	 	}
	 } 

}